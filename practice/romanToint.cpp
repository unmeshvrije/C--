#include <iostream>
using namespace std;

int main(int argc, char *argv[])
{
   int n =0;
   string s = "DCXXI";
   map<char,int> romap;
   romap.insert(map<char,int>('I', 1);
   romap.insert(map<char,int>('V', 5);
   romap.insert(map<char,int>('X', 10);
   romap.insert(map<char,int>('L', 50);
   romap.insert(map<char,int>('C', 100);
   romap.insert(map<char,int>('D', 500);
   romap.insert(map<char,int>('M', 1000);

   for (int i = 0; i < s.size(); ++i)
     cout << s.at(i) << endl;

     for(int i = 0; i < s.size(); ++i) {
        if (i+1 < s.size() && romap[s[i]]
    }

   cout << n << endl;
   return 0;
}
